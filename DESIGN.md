# Design System

## Direction

Richology 能力图谱采用“双重能力世界”方向，两种视图共享数据、状态和交互语义：

- **星图**：近黑色空间场承载 3D 节点与关系，适合精确查看能力连接与完整前置链。
- **地形**：矿物纸色界面承载一块抽象的 3D 成长地貌，4 个领域成为相连区域，适合理解能力群、跨领域通道和整体成长结构。

两者不是明暗主题，也不是两套内容。切换只改变空间表达，当前筛选、选中节点、详情面板和浏览历史必须保持。视觉应理性、可信、有生命力；地形使用程序化抽象地貌，不伪装成真实地理，也不暗示唯一成长路线。

视觉参考只取两类原则：

- `richology.cn` 的品牌锚点：墨黑、青蓝、酸性青柠，以及黑白编辑感。
- Marble Curriculum 的交互拓扑：单页图谱、旋转缩放、筛选、节点聚焦与关系追溯。不得复制其品牌、配色、字体、资产、文案或代码。

## Color

### Brand anchors

| Token | Value | Use |
|---|---:|---|
| `--ink` | `#111114` | 页面与图谱主背景 |
| `--cyan` | `#0CA8D4` | Richology 主交互色、AI 技能化 |
| `--lime` | `#C5EE4C` | Richology 活力色、商业化 |
| `--paper` | `#F1F1EF` | 主文字与浅色信息面 |

### Graph domains

| Domain | Color | Marker |
|---|---:|---|
| AI 技能化 | `#0CA8D4` | 实心圆 |
| 商业化 | `#C5EE4C` | 双环圆 |
| 职业化 | `#FF7664` | 菱形 |
| 社会化 | `#F5C325` | 六边形 |

领域识别不得只依赖颜色；筛选器、图例、焦点标签和节点轮廓共同编码。正文使用 `#F1F1EF`，次级文字使用不低于 AA 对比度的冷灰；青柠色不承载浅底小字号正文。

## Typography

- 品牌文字：`Richology`，暂不使用图形 Logo。
- 中文界面：系统无衬线字体栈，优先 `PingFang SC`、`Microsoft YaHei`、`Noto Sans CJK SC`。
- 数字与节点 ID：系统等宽字体栈。
- 大标题允许紧凑字距；正文保持正常字距和 1.6 左右行高。
- 不加载第三方远程字体，减少隐私、性能与中文缺字风险。

## View Switch

- 桌面端在图谱上方提供紧凑的 `星图 / 地形` 分段控制；移动端放入顶部工具条。
- 默认进入星图。用户主动切换后，在当前会话中记住选择。
- 切换动画控制在 500–800ms，通过相机与节点位置插值完成，不做整页淡出。
- 减少动态模式下即时切换，不做空间形变。
- 地形视图常驻说明：`地形用于呈现能力结构，不代表固定等级或唯一路径。`

## Shape and Surface

- 主页面为连续空间，不使用成排同尺寸圆角卡片。
- 星图以深色空间为主；地形视图切换为矿物纸色外框与深色地貌，详情面板保持相同信息结构。
- 详情面板是唯一主要浮层：桌面端从右侧进入，移动端为底部抽屉。
- 圆角克制：控制按钮 8–12px，详情面板 16px；不使用胶囊作为所有容器。
- 边框和阴影用于分层，不用大面积玻璃拟态。

## Motion

- 拖动旋转、滚轮或双指缩放；释放后仅保留短促惯性。
- 默认不自动持续旋转。首次进入可有一次 600–900ms 的轻微定向运动，帮助理解空间性。
- 节点选择使用位置保持、亮度和线条显隐，不使用弹跳或大幅缩放。
- 星图与地形切换使用同一组节点 ID 插值，切换后节点选择与关系高亮不丢失。
- `prefers-reduced-motion: reduce` 下关闭惯性、入场运动和缓动相机，只保留即时状态切换。

## Relationship Language

| Type | Default | Focused state | Chinese label |
|---|---|---|---|
| `requires` | 细实线 | 高对比实线，终点箭头 | 需要 |
| `supports` | 短虚线 | 高对比短虚线 | 支持 |
| `related_to` | 点线 | 高对比点线，无方向 | 相关 |

对于 `requires`，数据语义为“`source_id` 需要 `target_id`”。默认全局视图隐藏箭头以控制噪声；聚焦节点或打开关系说明时才显示指向 `target_id` 的箭头与文字说明。

## Content Voice

- 先说用户能看见和做什么，再解释方法。
- 使用“能力正在形成”“当前证据显示”，避免“孩子就是”“天赋类型”等固定化表达。
- 明确 `draft` 状态、数据版本与使用边界。
- 面向用户的界面文案使用中文；`Richology`、节点 ID、许可证名和必要技术术语可保留原文。

## Accessibility

- WCAG 2.2 AA；键盘焦点清晰可见。
- Canvas 同步提供语义化节点列表、当前选择摘要和 `aria-live` 状态。
- 键盘可在可见节点间移动、选择、返回和重置视角。
- 触控目标最小 44×44 CSS px；详情抽屉不遮断关闭与返回操作。
- 高对比模式下降低背景装饰，保留节点轮廓、标签和关系线型。
